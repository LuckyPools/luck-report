<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luck.report.agent.modules.chat.mapper.ChatMessageMapper">

    <!-- 根据会话ID查询消息列表，按创建时间升序排列 -->
    <select id="selectBySessionId" resultType="com.luck.report.agent.modules.chat.domain.entity.ChatMessage">
        SELECT id, session_id, role, content, message_type, metadata, create_time
        FROM luck_chat_message
        WHERE session_id = #{sessionId}
        ORDER BY create_time ASC
    </select>

    <!-- 根据ID查询消息 -->
    <select id="selectById" resultType="com.luck.report.agent.modules.chat.domain.entity.ChatMessage">
        SELECT id, session_id, role, content, message_type, metadata, create_time
        FROM luck_chat_message
        WHERE id = #{id}
    </select>

    <!-- 查询会话的消息数量 -->
    <select id="countBySessionId" resultType="int">
        SELECT COUNT(*) FROM luck_chat_message WHERE session_id = #{sessionId}
    </select>

    <!-- 插入单条消息，createTime由Java侧赋值，ID由Snowflake生成 -->
    <insert id="insert">
        INSERT INTO luck_chat_message (id, session_id, role, content, message_type, metadata, create_time)
        VALUES (#{id}, #{sessionId}, #{role}, #{content}, #{messageType}, #{metadata}, #{createTime})
    </insert>

    <!-- 批量插入消息，createTime由Java侧赋值，ID由Snowflake生成 -->
    <insert id="batchInsert">
        INSERT INTO luck_chat_message (id, session_id, role, content, message_type, metadata, create_time) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.sessionId}, #{item.role}, #{item.content}, #{item.messageType}, #{item.metadata}, #{item.createTime})
        </foreach>
    </insert>

    <!-- 根据ID删除单条消息 -->
    <delete id="deleteById">
        DELETE FROM luck_chat_message WHERE id = #{id}
    </delete>

    <!-- 根据会话ID删除所有消息 -->
    <delete id="deleteBySessionId">
        DELETE FROM luck_chat_message WHERE session_id = #{sessionId}
    </delete>

</mapper>
