# dev 环境配置（vite 自动加载 .env.dev）
# 注意：Vite 默认会将以 VITE_ 开头的变量注入到 import.meta.env
# 端口号与公共路径无 VITE_ 前缀，仅供 vite.config.ts 通过 loadEnv(...) 读取
VITE_APP_NAME=LUCK报表平台
VITE_API_BASE_URL=http://localhost:8050
port=8081
VITE_PUBLIC_PATH=/luck-report/
