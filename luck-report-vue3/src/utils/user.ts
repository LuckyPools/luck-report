/**
 * 用户工具模块
 * 提供当前登录用户信息的获取方法，作为与业务系统对接的钩子
 * 后续接入账号体系后，替换为真实的用户信息获取逻辑
 */

/**
 * 获取当前用户ID
 * 当前返回固定值 1，后续对接业务系统后替换为真实逻辑
 * 所有需要 userId 的接口调用统一通过此方法获取，确保切换时只需改一处
 *
 * @returns 当前用户ID
 */
export function getUserId(): number {
  return 1
}
